#include "file_header.h"
int main()
{
    // info_t *data;
    // int data_count;

    // data=(info_t*)(calloc(5,sizeof(info_t)));

    // FILE *fptr=fopen("data.csv","r");
    
    // error_t able_to_open=openfile(fptr);
    
    // errno_t store_data=storedata(fptr,data,&data_count);

    // errno_t display=displaydata(data,&data_count);
    // errno_t sorted_data=sortdata(data,&data_count);
    // display=displaydata(data,&data_count);
    test_main();
    return 0;

}